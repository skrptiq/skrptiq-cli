## Push Summary — skrptiq-cli (main)

### What shipped
Wired remaining read-side slash commands to real engine data:

- `/runs list` — queries executions table, shows status (✓/✗/◌/⏸), workflow name, timestamp, token count, errors
- `/profile use <name>` — actually switches the active profile in the DB, updates prompt and status bar live
- `/hub search <query>` — real Hub API search via engine's hubapi.Client, shows results with categories and descriptions
- Engine layer: added ListExecutions, GetExecution, GetSteps, SetActiveProfile, FindProfileByName, Hub client

### Still deferred (write/execution operations)
- `/run` — execute workflow
- `/resume`, `/stop`, `/status` — execution control
- `/hub import`, `/hub update` — hub write operations
- `/mcp connect/disconnect/tools` — MCP connection management
- `/providers add`, `/workspace set`, `/config set`, `/dials set`

### Tests
- `go test ./...` — all passing
- `go vet ./...` — clean
