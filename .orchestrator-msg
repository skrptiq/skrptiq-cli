## Push Summary — skrptiq-cli (main)

### What shipped
- Execution now runs in goroutines — no longer blocks bubbletea's render loop
- Chat streaming, workflow execution, and gate resume all run async
- Progress events print via Program.Println from goroutines (safe — outside renderer lock)
- Prompt stays responsive during execution
