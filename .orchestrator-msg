## Push Summary — skrptiq-cli (main)

### What shipped
- `/command` now returns to command mode from chat or run (alongside `/exit`)
- `/run` moved into handleSlashCommand — works reliably from any mode, with or without a workflow name
- All mode commands (/chat, /run, /command, /exit) are in the autocomplete registry
