## Push Summary — skrptiq-cli (main)

### What shipped
1. **Input text wrapping** — switched from textinput (single-line, no wrap) to textarea (wraps up to 6 lines). Long input text is now visible. Enter still submits (newline insertion disabled).
2. **Scroll during execution** — UpdateLastOutput now respects scroll position. If the user has scrolled up with pgup, new output doesn't yank them back to bottom. pgup/pgdown always work, even during active streaming.
