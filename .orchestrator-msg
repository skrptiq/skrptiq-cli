## Push Summary — skrptiq-cli (main)

### What shipped
- **Single enter to submit** — enter always submits immediately, never blocked by autocomplete. Tab selects from autocomplete, enter submits the input as typed.
- **Alphabetically sorted autocomplete** — all completion lists (commands, subcommands, arguments) are sorted alphabetically for easier scanning.
