## Push Summary — skrptiq-cli (main)

### What shipped
- Fixed tab completion for multi-word commands. `/hub l` + tab now correctly completes to `/hub list`.
- Autocomplete filter now matches the full input against multi-word command names (e.g. typing `/hub l` filters to `/hub list`).
- Fixed arg parsing to use `LastIndex` for space splitting so argument completers trigger at the right boundary.

### Tests
- `go test ./...` — all passing
- `go vet ./...` — clean
