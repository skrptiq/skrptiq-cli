## Push Summary — skrptiq-cli (main)

### What shipped
Autocomplete selections that don't need more input now auto-submit immediately — no second enter needed. Selecting `/chat` from autocomplete enters chat mode in one action. Same for `/help`, `/clear`, `/exit`, and any final argument selection.
