## Push Summary — skrptiq-cli (main)

### What shipped
- Typing `/` and enter shows a formatted command list with descriptions and subcommands (replaces the flat readline completion display)
- Tab still works for inline completion, but `/` gives the readable reference
