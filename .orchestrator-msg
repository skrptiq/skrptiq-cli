## Push Summary — skrptiq-cli (main)

### What shipped
Prompt now initialises through enterMode(ModeCommand) so the default command mode prompt is always consistent — profile name left, exit hint right, ❯ symbol.
