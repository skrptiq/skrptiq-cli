## Push Summary — skrptiq-cli (main)

### What shipped
- Workflow name tab completion in run mode — type part of a workflow name, tab to complete
- /run without a name enters run mode where bare text is treated as workflow name
- Tab-completing a workflow name auto-submits and starts execution
- Bare text completer system — reusable pattern for mode-specific completions
- Refactored execution start into shared handleEnterRunExec function
